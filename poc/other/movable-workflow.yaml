id: movable-workflow

info:
  name: Movable Type Security Checks
  author: dhiyaneshDk
  description: A simple workflow that runs all Movable related nuclei templates on a given target.

workflows:
  - template: default/default-movable-page.yaml
    subtemplates:
      - template: cve/CVE-2021-20837.yaml

  - template: auth/movable-type-login.yaml
    subtemplates:
      - template: cve/CVE-2021-20837.yaml
