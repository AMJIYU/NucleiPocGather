id: openam-workflow

info:
  name: OpenAM Security Checks
  author: daffainfo
  description: A simple workflow that runs all OpenAM related nuclei templates on a given target.

workflows:
  - template: detect/openam-detect.yaml
    subtemplates:
      - tags: openam
