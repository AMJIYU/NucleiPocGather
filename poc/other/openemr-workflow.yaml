id: openemr-workflow

info:
  name: OpenEMR Security Checks
  author: daffainfo
  description: A simple workflow that runs all OpenEMR related nuclei templates on a given target.

workflows:
  - template: detect/openemr-detect.yaml
    subtemplates:
      - tags: openemr
