id: mantisbt-workflow

info:
  name: MantisBT Security Checks
  author: daffainfo
  description: A simple workflow that runs all MantisBT related nuclei templates on a given target.

workflows:
  - template: other/mantisbt-panel.yaml
    subtemplates:
      - tags: mantisbt
