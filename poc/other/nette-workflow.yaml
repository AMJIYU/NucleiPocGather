id: nette-workflow

info:
  name: Nette Framework Security Checks
  author: daffainfo
  description: A simple workflow that runs all Nette Framework related nuclei templates on a given target.

workflows:
  - template: detect/tech-detect_15.yaml
    matchers:
      - name: nette
        subtemplates:
          - tags: nette
