id: novnc-workflow

info:
  name: noVNC Security Checks
  author: daffainfo
  description: A simple workflow that runs all noVNC related nuclei templates on a given target.

workflows:
  - template: detect/tech-detect_15.yaml
    matchers:
      - name: novnc
        subtemplates:
          - tags: novnc
