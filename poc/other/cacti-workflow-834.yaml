id: cacti-workflow

info:
  name: Cacti Checks
  author: pikpikcu
  description: A simple workflow that runs all Cacti related nuclei templates on a given target.

workflows:

  - template: http/exposed-panels/cacti-panel.yaml

    subtemplates:
      - template: http/vulnerabilities/other/cacti-weathermap-file-write.yaml
