id: saprouter-workflow
info:
  author: randomstr1ng
  description: A simple workflow that runs all checks related to SAProuter.
  name: SAProuter Security checks
  tags: workflow
workflows:
- subtemplates:
  - template: sap/saprouter-information-leak.yaml
  template: sap/saprouter-detect.yaml
